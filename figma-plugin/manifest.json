{
  "name": "HTML to Figma Importer",
  "id": "0000000000000000000",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": [
    "figma"
  ],
  "documentAccess": "dynamic-page",
  "networkAccess": {
    "allowedDomains": [
      "*"
    ],
    "reasoning": "Calls the capture service that renders a page for import. The address is set by the user under Settings — usually http://localhost:8787, or a server they host themselves — so the domain cannot be known in advance. The plugin contacts nothing else."
  }
}
